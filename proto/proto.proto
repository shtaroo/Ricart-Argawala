syntax = "proto3";
option go_package = "Ricart-Argawala/grpc/proto";

message GeneralServer {
    string from = 1;
    string to = 2;
    string value = 3;
    bool agree = 4;
}

service Node {
    rpc GeneralCommunication (stream GeneralServer) returns (stream GeneralServer);
}